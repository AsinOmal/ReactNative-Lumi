rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && request.auth.token.admin == true;
    }

    // Pack cover art — card thumbnail; uploaded by admin panel, read by mobile.
    match /covers/{file} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // Pack gate art — full-bleed background for PackGate / PremiumPackGate.
    match /gates/{file} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // Pack detail art — full-bleed background for PackDetailScreen.
    match /details/{file} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // 3D model GLB files — uploaded by admin panel, read by mobile app for AR.
    match /models/{file} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // Word pronunciation audio — uploaded by admin panel, read by mobile app.
    match /audio/{file} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }
  }
}
