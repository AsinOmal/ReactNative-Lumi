import { StyleSheet } from 'react-native';
import { colors } from '../../constants/colors';

export const styles = StyleSheet.create({
  card: {
    backgroundColor: '#FFFFFF',
    borderRadius: 26,
    borderWidth: 2,
    borderColor: '#C48A4A',
    overflow: 'hidden',
    shadowColor: '#3D2008',
    shadowOffset: { width: 0, height: 5 },
    shadowOpacity: 0.14,
    shadowRadius: 12,
    elevation: 4,
  },
  header: {
    aspectRatio: 1,
    overflow: 'hidden',
    backgroundColor: colors.primaryLight,
  },
  iconWrap: { flex: 1, alignItems: 'center', justifyContent: 'center' },
  cornerCheck: {
    position: 'absolute',
    top: 10,
    right: 10,
    backgroundColor: 'rgba(34,197,94,0.92)',
    borderRadius: 12,
    padding: 4,
  },
  lockedDim: {
    ...StyleSheet.absoluteFillObject,
    backgroundColor: 'rgba(0,0,0,0.42)',
  },
  lockedChipWrap: {
    ...StyleSheet.absoluteFillObject,
    alignItems: 'center',
    justifyContent: 'center',
  },
  lockedChip: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
    paddingHorizontal: 14,
    paddingVertical: 8,
    borderRadius: 18,
    backgroundColor: colors.primary,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 3 },
    shadowOpacity: 0.35,
    shadowRadius: 6,
    elevation: 4,
  },
  lockedChipText: {
    fontFamily: 'Fredoka-Bold',
    fontSize: 14,
    color: '#FFFFFF',
  },
  footer: { padding: 14, gap: 7 },
  name: {
    fontFamily: 'Fredoka-Bold',
    fontSize: 26,
    lineHeight: 30,
    color: colors.textDark,
    letterSpacing: 0.2,
  },
  subtitle: {
    fontFamily: 'Fredoka-Regular',
    fontSize: 14,
    color: colors.textMid,
  },
  progressTrack: {
    height: 8,
    borderRadius: 4,
    backgroundColor: '#EEE7D8',
    overflow: 'hidden',
    marginTop: 4,
  },
  progressFill: { height: 8, borderRadius: 4 },
  metaRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginTop: 4,
  },
  wordCount: {
    fontFamily: 'Fredoka-SemiBold',
    fontSize: 14,
    color: colors.textMid,
  },
  pill: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 5,
    paddingHorizontal: 12,
    paddingVertical: 5,
    borderRadius: 999,
    borderWidth: 1,
  },
  pillFree: { backgroundColor: '#E8F8D9', borderColor: '#B9E8B5' },
  pillPremium: { backgroundColor: '#FEF3C7', borderColor: '#F4D78A' },
  pillText: { fontFamily: 'Fredoka-Bold', fontSize: 13 },
  pillTextFree: { color: '#2E9E58' },
  pillTextPremium: { color: '#92400E' },
});
